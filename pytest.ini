# -----------------------------
# pytest.ini
# -----------------------------
[pytest]
minversion = 7.0
addopts = -ra -q
asyncio_mode = auto
testpaths =
    tests
python_files =
    test_*.py
    *_test.py
